# wgame-fs

Cross-platform file reading utilities.

Provides async file reading for both desktop (std) and web platforms.

## Features

- Async file reading on desktop with `async-fs`
- File fetching on web with `fetch` API
- Unified API for both platforms
- Byte and string reading operations

