# wgame-gfx-texture

Texture atlases, gradients, filtering, and partial updates. `Texture::update` and `update_part` maintain filtering borders; atlas growth keeps handles valid. See [textures](../docs/GUIDE.md#textures-and-files). Rebuild baked scenes after atlas layout changes.
